import { Module } from '@nestjs/common';

// modulo vacio por ahora la consigna pide los 3 modulos del back
// sprint 2 le metemos el schema controller y service de publicaciones
@Module({})
export class PublicacionesModule {}